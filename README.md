# 月子食谱助手
科学生成月子期间每日食谱

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Set the `DS_API_KEY` in [.env.local](.env.local) to your DeepSeek API key
4. Run the app:
   `npm run dev`
